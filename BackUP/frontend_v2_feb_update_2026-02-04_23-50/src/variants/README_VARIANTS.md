# 🚀 Як створювати нові Варіанти (Кампанії)

Ця інструкція допоможе тобі додати новий рекламний шлях (наприклад, `/love`) за 3 простих кроки.

## Крок 1: Створити файл конфігурації
1. Перейди в папку `src/variants/themes/` (для тем) або `src/variants/months/` (для місяців).
2. Скопіюй файл `template.js` і назви його відповідно до назви кампанії (наприклад, `love.js`).

## Крок 2: Заповни дані
Відкрий свій новий файл і впиши тексти:
- `id`: Має точно співпадати з назвою файлу (наприклад, `'love'`). Це і буде твоя адреса: `site.com/love`.
- `ui`: Твої заголовки та колір фону.
- `aiContext`: Підказка для ШІ (що саме він має врахувати).
- `pricing`: Якщо хочеш змінити ціну ТІЛЬКИ для цієї сторінки.

## Крок 3: Зареєструй варіант
Відкрий файл `src/variants/index.js` і додай свій новий файл у список:

```javascript
import { loveConfig } from './themes/love.js'; // 1. Імпортуй

export const VARIANTS = {
    'february': februaryConfig,
    'love': loveConfig, // 2. Запиши сюди
};
```

---
💡 **Порада**: Якщо боїшся помилитися в коді — просто поклич мене і скажи: *"Друже, створи варіант /promo з таким-то заголовком"*, і я все зроблю за секунду.
